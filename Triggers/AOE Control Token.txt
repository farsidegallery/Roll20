!script {{
  --/|Looks for AoEControlToken and moves it to the top of the Z-order.
  
  --/|TRIGGER_REPLACEMENTS
  --#hideCard|1
  --&name|[&GraphicNewname]
  --?"[&name]" -eq "AoEControlToken"|>changeorder
  --X|
  
 --:changeorder|
  --/|Find any tokens on the page that match
  --~count|array;pagetokens;tokens;[&GraphicNew_id];tprop:name=[&name]

  --/|Loop through the matching tokens and use the z-order tofront command on them
  --%loop|foreach;tokens
   --z:graphic:[*[&loop]:t-id]|tofront  {&delay 1.5}
   --*|Moved to Top.
  --%|
   --<|
}}